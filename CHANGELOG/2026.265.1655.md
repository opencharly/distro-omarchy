# 2026.265.1655 — feat(omarchy): own the omarchy family skills in distro-omarchy

## Summary

Move the 3 Omarchy-family `skill:` entities out of the
`layer-charly-*` concept aggregators into this repo — the repo that owns the
artifacts they document. Each entity is copied verbatim, with `owner:` corrected
to the skill's own name (the convention every single-candy repo uses).

| Skill | family | was in |
|---|---|---|
| `omarchy-cstream` | `distros` | `layer-charly-distros` |
| `omarchy` | `distros` | `layer-charly-distros` |
| `omarchy-vm` | `vm` | `vm-charly-vm` |

Files changed (`git diff --stat origin/main`):

```
CHANGELOG/2026.265.1652.md |  64 +++++++++++
 charly.yml                 | 268 +++++++++++++++++++++++++++++++++++++++++++++
 2 files changed, 332 insertions(+)
```

## How tested

Config-class pre-flight `charly box validate` over THIS repo's tree, plus the
generator gate (the ONLY consumer of a `skill:` entity):

```
$ charly box validate
charly box validate: OK — 0 warnings, 0 errors
```

Byte-identity of the published corpus: a scratch marketplace project whose candy
files are the exact entities this PR adds re-emitted every moved skill
byte-identical to the committed corpus:

```
byte-identical=35 missing=0 differs=0   # all wave producer entities, incl. this repo's
```

The emitter is a pure function of the entity fields
(`candy/plugin-marketplace` `emit_skills.go`).

## Wave / landing order

This is a PRODUCER in the skill-ownership wave; the companion marketplace pin and
the aggregator deletions land as their own PRs (bounded dual-source transient,
ended by the deletion PRs — the same sequence as the completed `distro-arch`
pilot: `distro-arch#39` → `marketplace#323` → `layer-charly-distros#10`).

## Rulebook compliance

- **R0** — `/charly-internals:skills` + `/charly-internals:marketplace` loaded.
- **R1** — `owner:` values corrected to the owning skill's name.
- **R2** — in-repo move complete; sibling landings scheduled.
- **R3** — producer half of de-duplication.
- **R4** — no sleeps/retries/workarounds.
- **R5** — aggregator copies deleted in the companion PRs.
- **R6** — dedicated checkout, PR-only, no force-push.
- **R7** — `charly box validate` + generator byte-identity on this branch.
- **R8/R9** — no labels/plans/schemas/generated files; no binary built here.
- **R10** — config class, `analysed on a live system`; no bed can compose a
  `skill:` entity.

## Change classification

Candy/box config (`skill:` entity fields in `charly.yml`) → `analysed on a live
system`. Proof: `charly box validate` clean; all moved skills re-emitted
byte-identically.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*