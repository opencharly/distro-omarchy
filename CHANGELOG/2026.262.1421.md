# 2026.262.1421 — feat(vm): omarchy-vm on the 4.0.4 ISO + reconcile the pin prose

## Summary of changes

Re-points the `omarchy-vm` template from the 4.0.3 ISO to **4.0.4**, and reconciles the surrounding prose with what 4.0.4 actually ships (verified against the upstream 4.0.4 package list).

Concrete edits (1 file, +10/−9, four hunks):

- `charly.yml:215` — `omarchy-vm.vm.source.url` → `omarchy-4.0.4.iso` (checksum stays `type: sha256`, resolved from the publisher's `.sha256` sidecar).
- `charly.yml:963–976` (`check-omarchy-iso-vm` prose) — the bed now pins 4.0.4; its printing-stack assertions describe 4.0.4's `omarchy-base.packages`.
- `charly.yml:1147–1151` (`check-omarchy-iso-vm` browser-policy comment) — the unmeasured "Measured EMPTY on a stock 4.0.4 guest (0 entries)" claim is REPLACED by the check's actual basis: it asserts the DIRECTORY, not an entry count.
- `charly.yml:2216–2217` (`check-omarchy-desktop-vm` prose) — "the guest is omarchy 4.0.4". That bed derives from the SAME changed `omarchy-vm` template (`from: omarchy-vm`, `charly.yml:1754`), so it installs the changed 4.0.4 pin by construction.

**The 4.0.4 prose is measured, not carried over from 4.0.3.** Upstream 4.0.4's `install/omarchy-base.packages` printing stack (fetched from `basecamp/omarchy@v4.0.4`):

```
$ gh api repos/basecamp/omarchy/contents/install/omarchy-base.packages?ref=v4.0.4 --jq '.content' | base64 -d | grep -iE 'cups|print'
cups
cups-filters
cups-pk-helper
system-config-printer
```

That is exactly the set the prose names (`cups cups-filters cups-pk-helper system-config-printer`; `cups-browsed` + `cups-pdf` dropped).

## Change class

**candy/box/vm config** — a distro manifest (`charly.yml`) `kind: vm` template + its bed prose.

**Closes / relates to:** the ISO this template installs is proven by `check-omarchy-iso-vm` (below); the eval consumer is `opencharly/eval-omarchy#82`.

**Breaking change + rollback:** none — an ISO pin bump + prose. Revert restores 4.0.3.

## How R10-tested — exact gate, commands, and pasted output

- **The gate for this change class:** `charly box validate` + the composing VM bed `check-omarchy-iso-vm` (a `disposable: true` VM deploy that installs `omarchy-vm` from the pinned ISO).
- **Binary used:** `2026.262.1157` (the `deploy-add` log records `host charly provided at /tmp/charly-2026.262.1157-…`).
- **Eval bed run:** `check-omarchy-iso-vm | vm | ok | steps=10 | 1056s` (`disposable: true`), run-calver `2026.262.1344`.
- **Did the CHANGED code path execute live?** YES — the bed fetched the pinned ISO, sha256-verified it against the sidecar, installed it, and booted the guest; the 4.0.4-only printing assertion passed.
- **Concurrency (shared-state changes only):** `N/A — an ISO pin.`

**Evidence — put each item in the block whose label names it.**

*1. Fresh-rebuild gate output* (the frozen head, then `charly version` + the gate, at zero warnings):
```
$ git -C distro-omarchy rev-parse HEAD
c6404770c88582b55067b5e081dd0beff725e413
$ git log --oneline origin/main..HEAD
c640477 docs: re-freeze the body against the final head
84d9e06 docs(bed): replace the unmeasured browser-policy entry-count claim with the check's basis
221a8fc docs: re-freeze the body against the final head
72486db feat(vm): omarchy-vm on the 4.0.4 ISO + reconcile the pin prose

$ /tmp/opencode/charly-main/bin/charly version
2026.262.1157

$ cd <umbrella> && charly box validate -C distro-omarchy
charly box validate: OK — checked 178 candies, 21 boxes, 36 deploys, 7 distros, 7 builders; 0 warnings, 0 errors
```

*2. The composing VM bed, from `.check/check-omarchy-iso-vm/2026.262.1344/summary.yml`* (the run made on the binary named above; all 10 steps):
```
bed: check-omarchy-iso-vm
calver: 2026.262.1344
steps:
  - vm-build            ok: true   duration_seconds: 2
  - vm-create           ok: true   duration_seconds: 3
  - deploy-add          ok: true   duration_seconds: 21
  - bring-up-members    ok: true   duration_seconds: 0
  - check-live          ok: true   duration_seconds: 27
  - update              ok: true   duration_seconds: 792   # the fresh rebuild
  - rebuild-ready       ok: true   duration_seconds: 0
  - check-live-rebuild  ok: true   duration_seconds: 26
  - cleanup             ok: true   duration_seconds: 184
  - cleanup-members     ok: true   duration_seconds: 0
total_seconds: 1056
ok: true
diagnostics: {errors: 0, warnings: 0, advisories: 0}
```
The `deploy-add` step log records the exact binary the guest was deployed with:
```
$ grep 'host charly provided' .check/check-omarchy-iso-vm/2026.262.1344/deploy-add.log
guest charly absent/outdated; host charly provided at /tmp/charly-2026.262.1157-0389c2a766654e13 for deploy use
# => the bed ran on 2026.262.1157, the binary block 1 names. (The previous body
#    cited the older 2026.262.1059 run, which used a different binary — fixed here.)
```
The `check-live` step ran 64 assertions, all passing; the 4.0.4-specific one:
```
  PASS  check the printing packages 4.0.4 ships are installed  [omarchy-cups-filters]  installed
  PASS  check the managed browser-policy directory exists  [omarchy-iso-vm-chromium-policy-dir]  ok
64 steps: 64 passed, 0 failed, 0 skipped
```

*3. Docs-only evidence:* `N/A — not a docs-only change.`

## The R5 hard-cutover sweep (executed, raw, at the frozen head)

```
$ grep -n 'omarchy-4\.[0-9]*\.[0-9]*\.iso' charly.yml
215:            url: https://iso.omarchy.org/omarchy-4.0.4.iso
969:            # ported. This bed pins omarchy-4.0.4.iso, whose
# => the only ISO url in the tree is 4.0.4; NO 4.0.3/4.0.1 ISO url survives.

$ grep -n '4\.0\.[0-9]' charly.yml
215:            url: https://iso.omarchy.org/omarchy-4.0.4.iso
963:            # ── Omarchy's CUPS hardening — asserted (it landed in 4.0.3) ────────
966:            # down. On 4.0.1 none of that held (its omarchy-base.packages listed
969:            # ported. This bed pins omarchy-4.0.4.iso, whose
976:            - check: the printing packages 4.0.4 ships are installed
1001:            - check: the root CUPS-PDF backend is absent (the 4.0.3 hardening)
1011:            - check: automatic printer discovery is absent (the 4.0.3 hardening)
2217:            # omarchy 4.0.4 and the suite is read at quattro. A sequence tuned to quattro
2218:            # that lands elsewhere on 4.0.4 fails while the desktop is perfectly correct —
# Each remaining `4.0.3`/`4.0.1` mention is HISTORY: the CUPS hardening LANDED in
# 4.0.3, and 4.0.1 is the pre-hardening baseline the prose contrasts against.
# The line-1148 "0 entries" comment the previous body pasted NO LONGER EXISTS
# (it was replaced by the directory-basis comment at 1147-1151).
```

## Attribution tier

`fully tested and validated` — the composing VM bed installs the pinned 4.0.4 ISO end-to-end on a `disposable: true` target at zero warnings, on the binary block 1 names; the checksum provenance is the publisher's sidecar.

## Harness rulebook compliance — state HOW each is satisfied (or `N/A — <reason>`)

- **R0 skills:** `/charly-distros:omarchy`, `/charly-vm:vm`, `/charly-check:check` loaded.
- **R1 RCA + zero warnings:** the prose/reality divergence (4.0.4 facts) was resolved by fetching the upstream 4.0.4 list (R1) rather than carrying 4.0.3 text forward, AND an unmeasured browser-policy entry-count claim found during review was removed rather than papered over (R1 self-correction). The bed binary version now matches the named build (block 1/2). Zero warnings.
- **R2 no out-of-scope:** the pin + its prose are reconciled together.
- **R3 no duplication:** the template is the distro's own single home.
- **R4 no workaround:** the checksum stays sidecar-resolved.
- **R5 hard cutover + grep:** executed above; the only ISO url is 4.0.4; remaining 4.0.3 mentions are history.
- **R6 git safety:** append-only commits.
- **R7 runtime gate:** `check-omarchy-iso-vm` installs the ISO live.
- **R8 artifact invariants:** the seed/answers carry the pinned ISO.
- **R9 binary == source + deps:** the bed's `deploy-add` log records `charly-2026.262.1157`, matching `charly version`.
- **R10 disposable + coverage:** the bed is `disposable: true`.
- **RDD / ADE / SDD:** RDD — the 4.0.4 package list is measured before asserting it in prose.
- **Hard cutover:** atomic commit.
- **Kernel/plugin boundary law:** `N/A — an ISO pin in the distro manifest.`
- **Disposable-only autonomy:** the bed is `disposable: true`.
- **Clean architecture + Go gates:** `N/A — no Go; validate clean.`
- **CHANGELOG:** `N/A by construction — written at merge by charly-auto-merge[bot] from this body.`

---

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (fully tested and validated)*